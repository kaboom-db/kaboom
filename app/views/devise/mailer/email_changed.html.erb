<% paragraph = @resource.try(:unconfirmed_email?) ? "is being changed to #{@resource.unconfirmed_email}." : "has been changed to #{@resource.email}." %>
<%= render "shared/mailers/text", paragraphs: ["Hello, #{@resource.username}!", "We're contacting you to notify you that your email #{paragraph}", ""] %>
