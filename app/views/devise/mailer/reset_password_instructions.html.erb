<%= render "shared/mailers/text", paragraphs: ["Hello, #{@resource.username}!", "Someone has requested a link to change your Kaboom password. You can do this through the link below.", "If you didn't request this, you can safely ignore this email.", "Please note that your password won't change until you access the link below and create a new one."] %>
<%= render "shared/mailers/button", href: edit_password_url(@resource, reset_password_token: @token), text: "Change my password" %>
