<div class="my-6 flex flex-col gap-4">
  <h3 class="text-2xl">The ultimate comics tracker.</h3>
  <p>Kaboom is a database of comics. It contains information about comic series and individual issues. You can also create an account and track the issues that you have read.</p>
  <%= render LinkComponent.new(text: "Create your account!", href: new_registration_path(User)) %>
</div>
<div class="my-6 grid grid-cols-2 gap-4">
  <div class="col-span-1 p-1.5 rounded-lg bg-gradient-to-r from-primary to-secondary">
    <div class="flex flex-col max-h-full h-56 items-center justify-center">
      <h2 class="font-bold text-4xl">📚 1</h2>
    </div>
    <div class="p-4 bg-white rounded-lg flex flex-col gap-4">
      <h5 class="text-xl">Comics</h5>
      <p>We currently have 1 <%= "comic".pluralize(1) %> in our database!</p>
      <%= render LinkComponent.new(text: "Comics", href: "/") %>
    </div>
  </div>
  <div class="col-span-1 p-1.5 rounded-lg bg-gradient-to-r from-primary to-secondary">
    <div class="flex flex-col max-h-full h-56 items-center justify-center">
      <h2 class="font-bold text-4xl">😃 1</h2>
    </div>
    <div class="p-4 bg-white rounded-lg flex flex-col gap-4">
      <h5 class="text-xl">Users</h5>
      <p>Join 1 <%= "other".pluralize(1) %> in tracking their favourite comics!</p>
      <%= render LinkComponent.new(text: "Join now", href: new_registration_path(User)) %>
    </div>
  </div>
</div>
<div class="my-6">
  <h3 class="text-2xl">Apps & Integrations</h3>
  <div class="h-56 flex items-center justify-center">Coming soon</div>
</div>
